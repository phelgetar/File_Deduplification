"""Content extraction and classification.

Absorbed from two sibling projects when they were merged into this one:
  doc-classifier    extract.py, vision.py  (text/image -> plain text)
  File_Classifier   the Claude escalation path (now cloud.py)

engine.py is the ladder that ties them to core/classifier.py's rules.
"""
