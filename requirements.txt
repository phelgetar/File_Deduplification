# Core dependencies — everything needed for scanning, hashing,
# classification, organization, database caching, and notifications.
#
# For AI image content analysis (CLIP), also install:
#   pip install -r requirements-ai.txt

# Configuration
python-dotenv>=1.0.0
PyYAML>=6.0

# Database (MySQL via SQLAlchemy)
sqlalchemy>=2.0.21
pymysql>=1.1.0

# Document/media metadata extraction
pymupdf>=1.22.5
python-docx>=1.0.0
mutagen>=1.45.1

# Image metadata (EXIF/IPTC)
pillow

# Notifications (Slack webhook via HTTP)
requests

# GUI preview
PySimpleGUI>=4.60.5
